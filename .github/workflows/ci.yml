name: CI

on:
  push:
    branches:
      - 'fix/aurora-*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verify code
        run: python -m py_compile service.py
